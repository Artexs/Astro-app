---
const { session } = Astro.locals;
const isAuthenticated = !!session;
const buttonText = isAuthenticated ? "Go to My Flashcards" : "Get Started for Free";
const buttonUrl = isAuthenticated ? "/my-cards" : "/register";
---

<div class="relative w-full mx-auto p-4 sm:p-8">
  <div
    class="relative max-w-4xl mx-auto backdrop-blur-xl bg-gradient-to-b from-white/10 to-white/5 rounded-2xl shadow-2xl p-8 text-white border border-white/10"
  >
    <div class="space-y-8">
      <div class="text-center">
        <h1 class="text-6xl font-bold mb-4 text-gradient" data-testid="welcome-header">
          AI Flashcard Generator
        </h1>
        <p class="text-xl text-blue-100/90 drop-shadow-md" data-testid="intro-text">
          Automatically create study materials from your notes, articles, or documents. Let AI do the hard work, so you can focus on learning.
        </p>
      </div>

      <div class="flex justify-center">
        <a href={buttonUrl} class="inline-block bg-primary text-primary-foreground hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring disabled:pointer-events-none disabled:opacity-50 text-lg font-semibold px-8 py-4 rounded-lg shadow-lg transition-transform transform hover:scale-105">
          {buttonText}
        </a>
      </div>
    </div>
  </div>
</div>
